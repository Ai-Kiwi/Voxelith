use crate::render::RendererData;

pub fn render_gui(render_data : &mut RendererData) {
    //draw_fps();
    //draw_text("Epic sick game", 0.0, 50.0, 35.0, WHITE);
    //let mut triangle_count = 0;
    //for mesh in &render_data.chunk_submeshs_to_render {
    //    triangle_count += mesh.1.vertices.len();
    //}
    //triangle_count = triangle_count / 3;
    //draw_text(&format!("rendering {} triangles", triangle_count), 0.0, 100.0, 35.0, WHITE);
}